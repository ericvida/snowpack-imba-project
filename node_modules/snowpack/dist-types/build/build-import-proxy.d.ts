import { SnowpackConfig } from '../types/snowpack';
export declare function getMetaUrlPath(urlPath: string, isDev: boolean, config: SnowpackConfig): string;
export declare function wrapImportMeta({ code, hmr, env, isDev, config, }: {
    code: string;
    hmr: boolean;
    env: boolean;
    isDev: boolean;
    config: SnowpackConfig;
}): string;
export declare function wrapHtmlResponse({ code, isDev, hmr, config, }: {
    code: string;
    isDev: boolean;
    hmr: boolean;
    config: SnowpackConfig;
}): string;
export declare function wrapImportProxy({ url, code, isDev, hmr, config, }: {
    url: string;
    code: string;
    isDev: boolean;
    hmr: boolean;
    config: SnowpackConfig;
}): Promise<string>;
export declare function generateEnvModule(mode: 'development' | 'production'): string;
